[phases.setup]
nixPkgs = ["php82", "php82Extensions.mongodb", "php82Extensions.openssl"]
setup = [
    "pecl install mongodb",
    "docker-php-ext-enable mongodb"
]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT}"